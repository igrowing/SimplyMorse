tags:
  # Tests that decode real audio/video recordings. They need the
  # fixtures under test/assets/recordings and take longer than the
  # unit tests.
  video-recording:
  scoreboard:
  # Replays on-device debug CSVs dropped in the project root. Skips
  # itself when they are absent — they are developer captures, not
  # committed fixtures. See test/support/field_log_replay_test.dart.
  field-logs:
