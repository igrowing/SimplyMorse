name: simply_morse
description: "SimplyMorse - Audio-visual Morse code encoder-decoder."
publish_to: 'none'

version: 1.0.1+1

environment:
  sdk: ^3.12.2

dependencies:
  audioplayers: ^6.1.0
  cupertino_icons: ^1.0.8
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  get_it: ^8.0.3
  meta: ^1.15.0
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  torch_light: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^9.0.0

flutter:
  uses-material-design: true
